import BackLink from "@/components/dashboard/BackLink";

/** Shared breadcrumb on every handbook child page. */
export default function HandbookBack() {
  return <BackLink href="/dashboard/handbook">Handbook</BackLink>;
}
